#pragma once

#define BLOCK_SIZE_1_RATIO      ${block_size1_ratio}

#define MEM_SIZE                (${mem_size} * 1024 * 1024)
#define BLOCK_SIZE              4096

#define SRAND                   ${srand}
#define THREADS                 24